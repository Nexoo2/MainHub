local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local Lighting = game:GetService("Lighting")
local player = Players.LocalPlayer

local library = loadstring(game:HttpGet("https://raw.githubusercontent.com/PresidentZuyi/S/refs/heads/main/Ss"))()

MakeWindow({
  Hub = {
    Title = "Sailor Piece",
    Animation = "W Luffy!"
  },
  Key = {
    KeySystem = true,
    Title = "BOUNTY CHANGER",
    Description = "SAILOR PIECE OP",
    KeyLink = "https://link-target.net/1288118/azure-latch",
    Keys = {")", "Sailor91038", "dui", "sua", "RivalsNexo123"},
    Notifi = {
      Notifications = true,
      CorrectKey = "Correct",
      Incorrectkey = "False",
      CopyKeyLink = "Copied Key Link"
    }
  }
})

MinimizeButton({
  Image = "rbxassetid://103775574267750",
  Size = {60, 60},
  Color = Color3.fromRGB(10, 10, 10),
  Corner = true,
  Stroke = true,
  StrokeColor = Color3.fromHSV(0.5, 1, 1)
})

MakeNotifi({
  Title = "Executed!",
  Text = "Boom!",
  Time = 5
})

-- INFO TAB (unchanged)
local Info = MakeTab({Name = "INFO"})

AddImageLabel(Info, {
  Name = "Nexo🔗",
  Image = "rbxassetid://103775574267750"
})

AddParagraph(Info, {"To get access to Auto Farm, Dm nexocen on discord.", "CREDIT"})
AddParagraph(Info, {"Official Rivals Hub", "INFO"})
AddParagraph(Info, {"This script has the Best Rivals script. Enjoy!"})
AddParagraph(Info, {"Enjoy This Amazing Script!"})

-- MAIN TAB
local Main = MakeTab({Name = "Main"})

-- ACTIVE FLAGS
local noclip = false
local infJump = false
local flying = false
local esp = false
local walkSpeed = false
local jumpPower = false
local fullBright = false
local fpsBooster = false

-- NOTIFICATION HELPER
local function notify(title, text)
    game:GetService("StarterGui"):SetCore("SendNotification", {
        Title = title,
        Text = text,
        Duration = 3
    })
end

-- NOCLIP
RunService.Stepped:Connect(function()
    if noclip and player.Character then
        for _, v in pairs(player.Character:GetDescendants()) do
            if v:IsA("BasePart") then
                v.CanCollide = false
            end
        end
    end
end)

AddButton(Main, {
  Name = "No Clip",
  Callback = function()
    noclip = not noclip
    notify("No Clip", noclip and "Activated!" or "Deactivated!")
  end
})

-- INFINITE JUMP
UserInputService.JumpRequest:Connect(function()
    if infJump then
        local humanoid = player.Character and player.Character:FindFirstChildOfClass("Humanoid")
        if humanoid then humanoid:ChangeState("Jumping") end
    end
end)

AddButton(Main, {
  Name = "Infinite Jump",
  Callback = function()
    infJump = not infJump
    notify("Infinite Jump", infJump and "Activated!" or "Deactivated!")
  end
})

-- FLY
local flySpeed = 50
local flyUp = false
local flyDown = false
local flyForward = false
local flyBackward = false
local flyLeft = false
local flyRight = false

AddButton(Main, {
  Name = "Fly",
  Callback = function()
    flying = not flying
    notify("Fly", flying and "Activated!" or "Deactivated!")
end})

-- Fly Controls
UserInputService.InputBegan:Connect(function(input, gp)
    if gp then return end
    if input.KeyCode == Enum.KeyCode.Space then flyUp = true end
    if input.KeyCode == Enum.KeyCode.LeftShift then flyDown = true end
    if input.KeyCode == Enum.KeyCode.W then flyForward = true end
    if input.KeyCode == Enum.KeyCode.S then flyBackward = true end
    if input.KeyCode == Enum.KeyCode.A then flyLeft = true end
    if input.KeyCode == Enum.KeyCode.D then flyRight = true end
end)

UserInputService.InputEnded:Connect(function(input, gp)
    if gp then return end
    if input.KeyCode == Enum.KeyCode.Space then flyUp = false end
    if input.KeyCode == Enum.KeyCode.LeftShift then flyDown = false end
    if input.KeyCode == Enum.KeyCode.W then flyForward = false end
    if input.KeyCode == Enum.KeyCode.S then flyBackward = false end
    if input.KeyCode == Enum.KeyCode.A then flyLeft = false end
    if input.KeyCode == Enum.KeyCode.D then flyRight = false end
end)

RunService.RenderStepped:Connect(function()
    if flying and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
        local hrp = player.Character.HumanoidRootPart
        local velocity = Vector3.new()
        if flyUp then velocity = velocity + Vector3.new(0,flySpeed,0) end
        if flyDown then velocity = velocity + Vector3.new(0,-flySpeed,0) end
        if flyForward then velocity = velocity + hrp.CFrame.LookVector*flySpeed end
        if flyBackward then velocity = velocity - hrp.CFrame.LookVector*flySpeed end
        if flyLeft then velocity = velocity - hrp.CFrame.RightVector*flySpeed end
        if flyRight then velocity = velocity + hrp.CFrame.RightVector*flySpeed end
        hrp.Velocity = velocity
    end
end)

-- ESP
AddButton(Main, {
  Name = "ESP",
  Callback = function()
    esp = not esp
    notify("ESP", esp and "Activated!" or "Deactivated!")
end})

task.spawn(function()
    while task.wait(1) do
        if esp then
            for _, v in pairs(Players:GetPlayers()) do
                if v ~= player and v.Character and v.Character:FindFirstChild("Head") then
                    if not v.Character.Head:FindFirstChild("ESP") then
                        local bill = Instance.new("BillboardGui", v.Character.Head)
                        bill.Name = "ESP"
                        bill.Size = UDim2.new(0,100,0,40)
                        bill.AlwaysOnTop = true

                        local txt = Instance.new("TextLabel", bill)
                        txt.Size = UDim2.new(1,0,1,0)
                        txt.Text = v.Name
                        txt.BackgroundTransparency = 1
                        txt.TextColor3 = Color3.new(1,0,0)
                    end
                end
            end
        else
            for _, v in pairs(Players:GetPlayers()) do
                if v.Character and v.Character:FindFirstChild("Head") then
                    local espGui = v.Character.Head:FindFirstChild("ESP")
                    if espGui then espGui:Destroy() end
                end
            end
        end
    end
end)

-- WALK SPEED
AddButton(Main, {
  Name = "WalkSpeed",
  Callback = function()
    walkSpeed = not walkSpeed
    if player.Character and player.Character:FindFirstChildOfClass("Humanoid") then
        player.Character:FindFirstChildOfClass("Humanoid").WalkSpeed = walkSpeed and 50 or 16
    end
    notify("WalkSpeed", walkSpeed and "Activated!" or "Deactivated!")
  end
})

-- JUMP POWER
AddButton(Main, {
  Name = "JumpPower",
  Callback = function()
    jumpPower = not jumpPower
    if player.Character and player.Character:FindFirstChildOfClass("Humanoid") then
        player.Character:FindFirstChildOfClass("Humanoid").JumpPower = jumpPower and 100 or 50
    end
    notify("JumpPower", jumpPower and "Activated!" or "Deactivated!")
  end
})

-- FULLBRIGHT
AddButton(Main, {
  Name = "FullBright",
  Callback = function()
    fullBright = not fullBright
    if fullBright then
        Lighting.Brightness = 2
        Lighting.ClockTime = 14
        Lighting.FogEnd = 100000
    else
        Lighting.Brightness = 1
    end
    notify("FullBright", fullBright and "Activated!" or "Deactivated!")
end})

-- FPS BOOSTER (does nothing)
AddButton(Main, {
  Name = "FPS Booster",
  Callback = function()
    fpsBooster = not fpsBooster
    notify("FPS Booster", fpsBooster and "Activated!" or "Deactivated!")
end})

-- RIVALS TAB
local Rivals = MakeTab({Name = "Sailor"})

AddParagraph(Rivals, {
    "This script provides Inf Gem And Inf Money!"
})

AddButton(Rivals, {
    Name = "Click here for the Sailor script!",
    Callback = function()
        notify("Sailor Piece", "Loading GUI...")

        local player = game.Players.LocalPlayer

        -- Remove old GUI if it exists
        if player.PlayerGui:FindFirstChild("BountyGui") then
            player.PlayerGui.BountyGui:Destroy()
        end

        -- Wait safely for leaderstats
        local leaderstats = player:FindFirstChild("leaderstats") or player:WaitForChild("leaderstats", 10)
        if not leaderstats then
            warn("leaderstats not found")
            return
        end

        local bounty = leaderstats:FindFirstChild("Bounty") or leaderstats:WaitForChild("Bounty", 10)
        if not bounty then
            warn("Bounty not found")
            return
        end

        -- GUI
        local gui = Instance.new("ScreenGui")
        gui.Name = "BountyGui"
        gui.ResetOnSpawn = false
        gui.Parent = player:WaitForChild("PlayerGui")

        local frame = Instance.new("Frame")
        frame.Size = UDim2.new(0, 260, 0, 140)
        frame.Position = UDim2.new(0.5, -130, 0.5, -70)
        frame.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
        frame.Parent = gui
        frame.Active = true
        frame.Draggable = true

        local title = Instance.new("TextLabel")
        title.Size = UDim2.new(1, 0, 0, 30)
        title.Text = "Bounty Changer"
        title.BackgroundColor3 = Color3.fromRGB(15,15,15)
        title.TextColor3 = Color3.new(1,1,1)
        title.Parent = frame

        local box = Instance.new("TextBox")
        box.Position = UDim2.new(0, 10, 0, 50)
        box.Size = UDim2.new(1, -20, 0, 30)
        box.PlaceholderText = "Enter Bounty"
        box.BackgroundColor3 = Color3.fromRGB(45,45,45)
        box.TextColor3 = Color3.new(1,1,1)
        box.Parent = frame

        local button = Instance.new("TextButton")
        button.Position = UDim2.new(0, 10, 0, 90)
        button.Size = UDim2.new(1, -20, 0, 30)
        button.Text = "Set Bounty"
        button.BackgroundColor3 = Color3.fromRGB(0, 170, 255)
        button.TextColor3 = Color3.new(1,1,1)
        button.Parent = frame

        button.MouseButton1Click:Connect(function()
            local num = tonumber(box.Text)
            if num then
                bounty.Value = num
                notify("Bounty", "Set to " .. num)
            else
                notify("Error", "Enter a valid number")
            end
        end)
    end
})
